/**
 * 
 */
function addStudent(){
    
}

function schedule(){
    const testSchedule = new Schedule();
    console.log(testSchedule.selectedTime);
    testSchedule.trim(9.5, 12.0);
    console.log(testSchedule.selectedTime);
}